{
  "name": "amefys-web",
  "private": true,
  "description": "AMEFYS marketing site (static). No build step; tests run on plain Node.",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  }
}
